# BACKLOG - bolascan
src/tier_b.rs:120 | bug | high | mutate_swap_neighbor panics on subtraction underflow or index out of bounds when token has multi-byte chars and char count is 1 but byte count is >= 2. | check chars.len() < 2 instead of token.len() < 2 | status=done | FIXED: guard now checks chars.len() (collect chars first) instead of token.len() (byte length); a single 2-byte char no longer indexes chars[len-2] with len==1. Use chars.swap for clarity. Proving tests swap_neighbor_single_multibyte_char_does_not_panic (é/中/a/empty) + swap_neighbor_swaps_last_two_chars (abcd->abdc, aé->éa).
src/detector/id_param.rs:315 | bug | medium | mutate_id_in_url splits by '/' and replaces the last segment containing the ID, silently discarding query parameters if they are present. | FIXED: PathSegment arm now split_once(['?','#']) to peel off query/fragment FIRST, mutates only the path, then re-appends the exact original suffix. Proving tests mutate_path_id_preserves_query_string (?page=1&sort=asc kept, id 123->456) + mutate_path_id_preserves_fragment. | status=done
src/detector/id_param.rs:294 | bug | medium | mutate_json_id fails to traverse nested JSON structures when mutating a dotted field path like 'outer.user_id', leaving it unmutated. | FIXED: added parse_json_field_path (tokenizes keys + [i] indices exactly as extract_ids_recursive emits) and mutate_json_id now traverses the full accessor chain (object keys + array indices) to the leaf. Returns None on unresolvable path / non-mutatable leaf / non-integer new_id for numeric leaves (no more silently-unchanged body). Proving tests: mutate_json_nested_dotted_path (outer.user_id), mutate_json_array_indexed_path (items[1].id), mutate_json_root_array_path ([0].user_id), mutate_json_unresolvable_path_returns_none. Cargo.toml: added serde_json to dev-deps for the tests. | status=done
src/detector/mod.rs:180 | bug | medium | endpoint_for_probe uses .contains() to match the probe path with trimmed endpoint templates, causing incorrect resolution for substring/partial path templates. | FIXED: replaced the `.contains(prefix)` substring match (which mis-matched /superusers/5 to /users/{id}, matched EVERY path for an empty-prefix template, and ignored template segments after the first {) with a precise segment-wise path_matches_template: {param} matches one segment, literals must be byte-equal, segment counts must match. Also strips query/fragment first, and DROPS the arbitrary `.or_else(endpoints().next())` fallback - that misattributed a finding to a random endpoint AND forced compare_with_appmap instead of the correct compare_cross_role (the caller already handles None via cross-role, verified at mod.rs:78). Proving tests (path_match_tests): param/literal/segment-count/empty-template/multi-param cases. | status=done
src/detector/id_param.rs:61 | modernization | low | extract_resource_ids does not url-decode query parameter names or values before checking if they are IDs, failing to identify percent-encoded IDs. | FIXED: added a dependency-free percent_decode(%XX; malformed escapes left verbatim; `+` NOT treated as space so path components are untouched); query name+value are decoded BEFORE the is_id_param_name/is_resource_id checks so a percent-encoded id is recognized, while the RAW name/value are still stored (downstream mutation keeps operating on the URL's original encoded form). Proving tests percent_decode_decodes_hex_and_leaves_malformed_verbatim + extract_finds_percent_encoded_query_id_and_preserves_raw (?q=%31%32%33%34%35 -> 12345 detected, non-id param name so it isolates decoding). | status=done
src/detector/mod.rs:145 | silent-fallback | low | builder validation errors for Finding are mapped to BolaScanError::RolesFile, misclassifying the error. | FIXED: added a dedicated BolaScanError::FindingValidation(String) variant and the Finding::build() map_err now uses it instead of RolesFile - a finding-construction failure no longer masquerades as a roles-file error, so operators get an accurate diagnostic. | status=done
tests/adversarial.rs:47 | test-gap | low | The assertion uses || bad_uuid.len() != 36 where bad_uuid has length 32, making the assertion tautologically true and failing to actually test the behavior of is_resource_id(bad_uuid). | FIXED (but the suggested fix was WRONG): dropping `|| len != 36` and asserting `!is_resource_id` would FAIL — verified is_resource_id("550e8400-e29b-41d4-a71644665544") is TRUE (rejected by UUID branch len!=36, but ACCEPTED by the opaque-token branch: 32 chars >=20, all alphanumeric/-/_). Rewrote resource_id_uuid_wrong_hyphen_count_is_opaque_not_uuid to assert the truthful behavior (accepted as opaque, not UUID) + that a <20-char version is rejected, matching the sibling opaque-token test. Real assertions, no tautology. | status=done
tests/gap.rs:226 | test-gap | low | extract_json_ids_array_of_objects test contains an incorrect comment claiming it returns empty, and lacks any assertions to verify that elements inside root JSON arrays are actually extracted. | FIXED: the comment ("root array returns empty") was false — extract_ids_recursive DOES traverse root arrays. Replaced `let _ = ids;` with real assertions that ids contains ("[0].user_id","1") and ("[1].user_id","2"). | status=donesrc/tier_b.rs:28 | bolascan | silent-fallback | medium | REPOPULATE 2026-07-16 (main-thread deep review): `IdPatternRules::embedded()` parses the embedded Tier-B `id_patterns.toml` via `from_toml(include_str!(...)).unwrap_or_else(|_| Self { patterns: Vec::new() })`. If the shipped rule file fails to parse, it silently yields ZERO patterns; `classify_token` then matches nothing, so the BOLA scanner's ID classification degrades to detecting nothing with no operator-visible error - a silent recall collapse on the authoritative Tier-B rule source (Law 10 + Tier-B doctrine: a malformed rule file must fail loud, never ship an empty ruleset). | FIXED 2026-07-17: embedded() now panics with the serde error on a malformed compile-embedded TOML (`unwrap_or_else(|e| panic!("bolascan embedded tier_b/id_patterns.toml is malformed: {e}"))`) - a build/data invariant, fail-closed, never a zero-pattern ruleset. Strengthened embedded_patterns_load to assert the exact count (8) and specific names (was `!is_empty`); added from_toml_rejects_malformed_source negative twin (malformed TOML + missing `pattern` array both Err). `cargo test -p bolascan tier_b` green. | status=done
